# Private key of the deployer (no 0x prefix). Use a dedicated key with minimal funds.
PRIVATE_KEY=

# RPC URLs
BASE_MAINNET_RPC_URL=
BASE_SEPOLIA_RPC_URL=

# For contract verification on BaseScan (optional but recommended)
ETHERSCAN_API_KEY=
